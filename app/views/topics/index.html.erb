<div class="topic-index-wrapper">
  <div class="container">
      <div class="row">
        <div class="col-md-18">
           <% @topics.each do |topic| %>
             <div class="topic-index-single">
               <h2 class="topic-author">
                 <%= topic.user.name %>
               </h2>
                 <div class="col-md-4">
                   <%= image_tag topic.image.url %>
                   <%= topic.description %>
                   <%= link_to "投票", favorites_path(topic_id: topic.id), method: :post %>
                   <%= link_to "コメント", favorites_new_path %>
                   <%= topic.favorite_users.count %>
                 </div>   
                   <div class="col-md-4">
                     <%= image_tag topic.image.url %>
                     <%= topic.description%>
                     <%= link_to "投票", favorites_path(topic_id: topic.id), method: :post %>
                     <%= link_to "コメント", favorites_new_path %>
                     <%= topic.favorite_users.count %>
                   </div> 
                     <div class="col-md-4">
                       <%= image_tag topic.image.url %>
                       <%= topic.description %>
                       <%= link_to "投票", favorites_path(topic_id: topic.id), method: :post %>
                       <%= link_to "コメント", favorites_new_path %>
                       <%= topic.favorite_users.count %>
                     </div> 
                         <p class="topic-text">
                          <%= topic.description%>
                         </p>
                         <%= link_to "削除", topics_destroy_path(@topics), method: :delete %>
                          
             </div>
           <% end %>
        </div> 
      </div>
  </div>    
</div>         
          
         
           
            

